# Eternia Project Dependencies

# Testing
pytest==7.4.4
pytest-asyncio==0.23.5
hypothesis==6.98.8  # For property-based testing
pytest-benchmark==4.0.0  # For performance benchmarking
pytest-cov==4.1.0  # Enable --cov options used in pytest.ini

# Web Framework
fastapi==0.116.1
uvicorn==0.35.0  # ASGI server for FastAPI
httpx==0.28.1  # For testing FastAPI
pydantic==2.11.7
email-validator==2.3.0  # For Pydantic's EmailStr validation
sse-starlette==2.0.0
slowapi==0.1.9  # For API rate limiting
python-multipart==0.0.20  # For handling form data

# Data Processing
pyyaml==6.0.2
tomli==2.0.1  # Backport of tomllib for Python < 3.11

# Machine Learning
torch==2.8.0  # PyTorch for neural networks and machine learning

# Authentication
bcrypt==4.3.0  # For password hashing
PyJWT==2.10.1  # For JWT token handling

# Monitoring
prometheus-client==0.22.1  # For metrics collection and exposition
starlette-exporter==0.23.0  # For FastAPI/Starlette metrics

# Backup and Storage
boto3==1.40.28  # For AWS S3 integration

# Database
yoyo-migrations==8.2.0  # For database schema migrations

# Tracing / OpenTelemetry
opentelemetry-api==1.37.0
opentelemetry-sdk==1.37.0
opentelemetry-exporter-otlp-proto-grpc==1.37.0
opentelemetry-instrumentation-fastapi==0.58b0
opentelemetry-instrumentation-requests==0.58b0
opentelemetry-instrumentation-sqlite3==0.58b0
opentelemetry-instrumentation-logging==0.58b0

# System utilities
psutil==7.0.0

# WebSocket support for Starlette/Uvicorn
websockets==15.0.1

# Datasets for emotion data loader
datasets==4.0.0

# Uvicorn reload support
watchfiles==1.1.0

# Rate limiting dependency
limits==5.5.0
