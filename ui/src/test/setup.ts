// Vitest JSDOM setup file
// You can extend globals or configure test environment here if needed.

// Enable Testing Library / jest-dom matchers for Vitest
import '@testing-library/jest-dom/vitest';

// Example: silence console errors in tests to keep output clean (optional)
// const originalError = console.error;
// console.error = (...args: unknown[]) => {
//   originalError(...args);
// };
